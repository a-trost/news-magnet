{
  "name": "find-news-stories",
  "private": true,
  "workspaces": ["packages/*", "server", "client"],
  "scripts": {
    "dev": "bun run --filter '*' dev",
    "dev:server": "bun run --filter server dev",
    "dev:client": "bun run --filter client dev",
    "test": "bun run --filter '*' test"
  }
}
